name: Generate Snake Game from Contributions

on:
  workflow_dispatch:  # This allows you to manually trigger the workflow from GitHub UI.

jobs:
  generate-snake-game:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3  # Checks out your repository under $GITHUB_WORKSPACE, so your workflow can access it.

      - name: Generate Snake Game
        uses: platane/snk@sha256:1c8a0b51a75ad8cf36b7defddd2187bdbb92bbbb5521a9e6cc5df795b00fc590
        with:
          github_user_name: 'Sabbir-Hossain22'  # Specifies your GitHub username.
            
